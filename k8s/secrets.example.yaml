# Template only — DO NOT commit real secrets. Apply your own copy with real
# base64 values, or better, manage these via Sealed Secrets / External Secrets / Vault.
apiVersion: v1
kind: Secret
metadata:
  name: healthcare-db-secret
  namespace: healthcare-platform
type: Opaque
stringData:
  DB_NAME: healthplatform
  DB_USER: healthplatform
  DB_PASSWORD: REPLACE_ME
---
apiVersion: v1
kind: Secret
metadata:
  name: healthcare-app-secret
  namespace: healthcare-platform
type: Opaque
stringData:
  JWT_SECRET: REPLACE_ME_32_PLUS_RANDOM_BYTES
